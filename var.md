variable "bronze_schema_full_name" {
  description = "Full name of bronze schema (catalog.schema)"
  type        = string
  default     = "your_catalog_name.bronze"   # ← REPLACE with your catalog name
}